spring:
  application:
    name: nextlook-gateway
  profiles:
    active: ${SPRING_PROFILES_ACTIVE:dev}
  config:
    import: optional:configserver:${CONFIG_SERVER_URL:http://localhost:18888}

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics,prometheus
